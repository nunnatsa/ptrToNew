package ptr

import (
	"fmt"

	"k8s.io/utils/ptr"
)

func useDeref() {
	a := new(true) // want `replace ptr\.To\(true\) with the "new\(\)" built-in function: new\(true\)`
	if val := ptr.Deref(a, false); val {
		fmt.Println(val)
	}
}
